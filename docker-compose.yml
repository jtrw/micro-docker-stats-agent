services:
  docker-stats-agent:
    build: .
    container_name: docker-stats-agent
    restart: unless-stopped
    command: ["-interval=10s"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
